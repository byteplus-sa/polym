name: TEMPLATE-RENAME-ME
version: 0.1.0
stage: experimental
owners:
  - your-handle@

description_for_install: |
  One paragraph describing what a user gets when they install this skill.
  Shown by `super-skill list` and on install.

depends_on:
  skills: []
  binaries:
    - lark-cli: ">=1.0.20"
  env: []

io_contract:
  reads: []
  writes: []
  # Example for a producer skill:
  # writes:
  #   - target: supper-sa-wiki/write_queue
  #     schema: core/signal-envelope.schema.json
  #     signal_type: meeting

triggers: []

deprecates: []
