# AUTO-GENERATED by tools/build-registry.sh on every push to main.
# DO NOT HAND-EDIT — changes will be overwritten.
#
# Source of truth per skill: skills/<name>/manifest.yaml
#
generated_at: 2026-05-13T04:06:24Z

skills:
  - name: im-digest
    version: 0.1.0
    stage: beta
    description_for_install: |
      Pulls yesterday's Lark group-chat messages via lark-cli, organises them into
      topics and key content (customer feedback, feature asks, technical issues,
      decisions, pending items), then writes the digest to both the local wiki
      (raw snapshot + wiki entity pages) and the Lark SA Wiki (via write_queue).
      Runs daily to compound customer knowledge over time.
    owners: ['Carey8175']
    path: skills/im-digest
    triggers:
      - "整理昨天的消息"
      - "拉一下昨天的群消息"
      - "daily IM digest"
      - "im digest"
      - "帮我总结一下昨天的 IM"
      - "把昨天的聊天记录整理进 wiki"
      - "IM 消息整理"

  - name: local-wiki-init
    version: 0.1.0
    stage: beta
    description_for_install: |
      Initialize a local LLM-maintained wiki for an SA, inspired by Karpathy's
      LLM Wiki pattern. Creates the full directory structure (raw/, wiki/entities/,
      wiki/concepts/, wiki/interactions/, wiki/sources/), and writes SCHEMA.md,
      PROMPTS.md, README.md, and starter index/log files. The wiki is tuned for the
      BytePlus SA workflow: customers, products, and the relationships between them
      as first-class entities.
    owners: ['Carey8175']
    path: skills/local-wiki-init
    triggers:
      - "初始化本地 wiki"
      - "set up my local wiki"
      - "create my SA wiki"
      - "init local wiki"
      - "我想建一个本地知识库"
      - "local wiki"
      - "本地知识库"
      - "llm wiki"

  - name: sa-wiki
    version: 0.2.0
    stage: beta
    description_for_install: |
      BytePlus SA Team shared knowledge base — read and write the SA Wiki
      (Lark Bitable + Wiki pages). Supports 3-source parallel search (Wiki +
      Lark messages + BytePlus docs), compound knowledge accumulation, and a
      write_queue flow that is safe for concurrent multi-agent use.
      Requires lark-cli authenticated as the team user.
    owners: ['Carey8175']
    path: skills/sa-wiki
    triggers:
      - "SA wiki"
      - "我们的知识库"
      - "客户档案"
      - "查一下客户"
      - "帮我把这条信息归档到 wiki"
      - "整理进 wiki"
      - "write_queue"
      - "knowledge base"

profiles:
  - name: sa-mvp
    audience: SA
    description: |
      SA daily minimum set — three signal sources (meeting summary, IM digest,
      dashboard watch) flowing into sa-wiki, plus the wiki read/write skills.
      Validates the "data foundation as shared memory" thesis end-to-end.
    includes:
      - sa-wiki: "^0.2"
      - meeting-summary: "^0.1"
      - im-digest: "^0.1"
      - dashboard-watch: "^0.1"
      - customer-lookup: "^0.1"

